export namespace config {
    export namespace template {
        export type templateInfo = {
            mdDir: string,
            htmlDir: string
            template: {
                dir: string,
                index: string,
                article: string
            }
        }
    }
}